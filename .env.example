# 必須: python -c "import secrets; print(secrets.token_urlsafe(50))" で生成
SECRET_KEY=

# 開発: True / 本番: False
DEBUG=True

# カンマ区切りで許可するホストを列挙
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL を使う場合のみ設定（未設定なら SQLite を使用）
# DATABASE_URL=postgres://user:password@host:5432/dbname
